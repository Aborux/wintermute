// Simple message for Wintermute, basically an experiment.

option optimize_for = SPEED;

message SimpleMessage {
	required int32 id = 1;
	required string content = 2;

	enum Importance {
		LOW = 0;
		HIGH = 1;
		CRITICAL = 2;
	}

	optional Importance importance = 3;
}